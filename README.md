# Cute Timer App

A pastel green stopwatch-style timer with rounded controls, soft typography, and a green heart favicon.

## Run locally

Open `index.html` in your browser.

Or serve it with a local web server:

```bash
python3 -m http.server 8000
```

Then visit <http://localhost:8000>.
